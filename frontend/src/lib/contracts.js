/**
 * Optional on-chain wiring (placeholder).
 * For hackathon demo, voting/results are stored locally in `src/lib/storage.js`.
 *
 * If you have a deployed contract, you can:
 * - put the ABI here
 * - use ethers to call `castVote`, `getResults`, etc.
 */

export const CONTRACT_ADDRESS = import.meta.env.VITE_CONTRACT_ADDRESS;
