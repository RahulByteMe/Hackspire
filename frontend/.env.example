# Backend API base URL (no trailing slash).
# When set, adding a candidate uploads the image to the backend via POST /api/elections/:id/candidates.
# See docs/BACKEND_API.md for the expected API contract.
# VITE_API_URL=https://your-api.example.com

# Admin wallet addresses (comma-separated). Optional; can also set in src/config/admin.js
# VITE_ADMIN_ADDRESSES=0xYourAddress1,0xYourAddress2
